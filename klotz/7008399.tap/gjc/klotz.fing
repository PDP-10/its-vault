(defun finger (host)
       (setq fing-file (open (finger-device host) 'in))
       (do ((line (readline fing-file) (readline fing-file)))
	   ((null line) t)
	   (princ line)
	   (terpri)))

(defun finger-device (hostnam)
       (implode (append '(c h a :)
			(explode hostnam)
			'(/. n a m e))))
