/*  -*- C -*-

            Download program for the default GT40 scroller
              Written by Brian Schunck July 6, 1980

To compile and link this program

	.RUN C
	>-O GT40 GT40LD.C

The name GT40LD is used for the source and object files to avoid
conflict with the package of GT40 macros in GT40.MAC

*/

#include <STD.H>

#define SCROLLER "VT52.LDA"

_main() {
	if(!gtld(SCROLLER)) {
		errfmt("GT40: Cannot download %p\n", SCROLLER);
		exit(NO);
	}
	else putfmt("\fThe GT40 scroller file %p has been loaded\n",
		SCROLLER);

}
