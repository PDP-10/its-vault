;-*-stl-*-
;***********************************************************************
;*********************** Dynamic Fields for SCA ************************
;****** (c) copyright 1978 Massachusetts Institute of Technology *******
;***********************************************************************
;********************* Do not modify this file !!! *********************
;***********************************************************************


;This file contains routines for handling Dynamic Fields for SCA.

(declare (eval (read)))
(or (memq 'stl (status features)) (load '((stl) base)))

(global-open ((sca) dynfld))

;***?:
(global-read ((sca) mpoly))
(global-read ((sca) matrix))

(stlchar)

(declare (mapex t)
	 (genprefix scadynfld))

(defmesa dynamic-field
	 (arg ***)
	 ***
 )


(stlunchar)

(global-close)
