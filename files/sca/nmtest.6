;-*-lisp-*-
;
;This is a test file for SCA.

(declare (eval (read)))
(or (memq 'mode (status features)) (load '((sca) nmode)))

(global-read upoly)

(scachar)

(declare (mapex t)
	 (genprefix scatst))

(defmode-global *simple-poly*
		(sub (upolyf (*fixnum*))
		     (*term* *poly* *rpoly* *qrp*))
		(ret-types *term* *poly* *rpoly* *qrp*))

(scaunchar)

(global-print nmtest)
